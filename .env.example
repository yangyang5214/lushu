# 复制成 .env 使用（`pnpm dev` / `pnpm pages:dev` 会自动帮你复制）。
#
# .env 已在 .gitignore 中，绝不会被提交。这个 .env.example 才是提交进仓库的模板。
#
# 一个文件同时喂两边：
#   · 前端：只有 VITE_ 前缀的变量会被打进浏览器 bundle，其余不会被前端读到。
#   · Worker：`wrangler pages dev --env-file=.env` 会把下面的变量注入本地 Function。

# ── 前端（公开值）──────────────────────────────────────────
# Turnstile 的 site key 本来就是公开的（会出现在前端 bundle 里），不是密钥。
# 留空 = 不启用 Turnstile。想本地联调就填官方"永远通过"的测试 site key：
#   1x00000000000000000000AA
VITE_TURNSTILE_SITE_KEY=

# ── Worker（仅本地调试）────────────────────────────────────
# 配了 TURNSTILE_SECRET，新建路书就必须先过 Turnstile。
# 必须和上面的 site key 成对启用：只配一边会导致校验永远失败。
# 官方"永远通过"的测试 secret：
#   1x0000000000000000000000000000000AA
# TURNSTILE_SECRET=

# 全局路书名额软上限；0 表示不限。
MAX_BOOKS=20000

# 单本路书 JSON 字节上限（300 个地点约 33 KB）。
MAX_DOC_BYTES=262144
